# Tests

Run:

```bash
pytest -q
```

These tests validate the presence and basic schema of the evaluation outputs.

Add dataset-specific tests after connecting the existing notebooks/code to the actual data.
