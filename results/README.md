# Results

These files are output targets for the existing ML notebooks/scripts.

**Do not invent results.** Replace `[RUN]`, `[MODEL]`, `[VALUE]`, etc. only after executing the analysis.

Recommended final evidence:
- model comparison
- per-class metrics
- confusion matrix
- feature importance
- regression residual analysis
- clustering validation
